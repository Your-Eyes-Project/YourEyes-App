name: YourEyes

description: "Real-Time vision impaired AI assistant."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  image: ^3.0.8
  camera: ^0.10.0
  grpc: ^3.2.4
  protobuf: '>=2.0.0 <4.0.0'
  flutter_tts: ^3.8.3
  speech_to_text: ^6.3.0

dev_dependencies:

  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
